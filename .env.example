# Base URL of the FoodRush backend API.
# In development you can leave this unset — Vite proxies /api to
# http://localhost:5000 automatically (see vite.config.js).
# Set it explicitly when the backend is hosted elsewhere (e.g. Render/Railway):
VITE_API_URL=http://localhost:5000/api
